package me.ryanthetechman.Commands.Spawn;

/*
import org.bukkit.Bukkit;
import org.bukkit.Location;
import org.bukkit.entity.Player;
import org.bukkit.event.EventHandler;
import org.bukkit.event.EventPriority;
import org.bukkit.event.entity.EntityDamageEvent;
import org.bukkit.event.player.PlayerMoveEvent;
import org.bukkit.plugin.Plugin;
import org.bukkit.scheduler.BukkitTask;

import java.util.ArrayList;
import java.util.HashMap;
*/

public class CancelListener {// We are adding a cooldown to spawn right?
	/*
	 * HashMap<Integer, Player> ctime = new HashMap<Integer, Player>();
	 * 
	 * Plugin plugin; public CancelListener(Plugin passedPlugin) { this.plugin =
	 * passedPlugin; }
	 * 
	 * ArrayList<Player> cooldownPlayers = new ArrayList<Player>(); public
	 * boolean cooldown(Player p){ return true; }
	 * 
	 * @EventHandler(priority= EventPriority.HIGHEST, ignoreCancelled=true)
	 * public void onPlayerMove(PlayerMoveEvent event) { Location from =
	 * event.getFrom(); Location to = event.getTo(); if ((from.getBlockX() !=
	 * to.getBlockX()) || (from.getBlockY() != to.getBlockY()) ||
	 * (from.getBlockZ() != to.getBlockZ()) || (from.getWorld() !=
	 * to.getWorld())) { Player player = event.getPlayer(); if
	 * (this.playerTeleportLocation.get(player) != null) {
	 * ((BukkitTask)this.playerTeleportLocation.remove(player)).cancel();
	 * player.sendMessage(this.plugin.getMessage("messages.canceled"));//What is
	 * this? } } }
	 * 
	 * @EventHandler(priority=EventPriority.HIGHEST, ignoreCancelled=true)
	 * public void onPlayerDamage(EntityDamageEvent event) { if
	 * ((event.getEntity() instanceof Player)) { Player player =
	 * (Player)event.getEntity(); if (this.playerTeleportLocation.get(player) !=
	 * null) {
	 * ((BukkitTask)this.playerTeleportLocation.remove(player)).cancel();
	 * player.sendMessage(this.plugin.getMessage("messages.canceled")); } } }
	 */
}
